#include "include/base.h"
// NOTE(lucashdez): Delete this import later
#include <stdio.h>

int gtms_main() {
    int something = true;
    
}

int main(int argc, char **argv) {
    printf("something");
    something;
    return(0);
}